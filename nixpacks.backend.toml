# Attach to backend Railway service (Settings → Config file path)
[phases.setup]
nixPkgs = ["nodejs_20", "ffmpeg", "python3"]

[phases.install]
cmds = ["npm install"]

[start]
cmd = "bash scripts/start-railway.sh"
