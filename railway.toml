# aio-tools-frontend service — Root Directory: / (repo root)
[build]
buildCommand = "npm install && npm run build"

[deploy]
startCommand = "bash scripts/start-frontend.sh"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
