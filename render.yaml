# Render Blueprint — backend API + frontend
# https://render.com/docs/blueprint-spec
#
# MongoDB: set DATABASE_URL in the Render dashboard to your MongoDB Atlas connection string.
# Example: mongodb+srv://user:pass@cluster0.xxxxx.mongodb.net/utilitytools?retryWrites=true&w=majority

services:
  - type: web
    name: aio-tools-backend
    runtime: node
    buildCommand: npm install && npm run db:setup --workspace backend
    startCommand: bash scripts/start-backend.sh
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: NODE_ENV
        value: production
      - key: FRONTEND_URL
        value: https://utilitytools.in
      - key: PORT
        value: 10000

  - type: web
    name: aio-tools-frontend
    runtime: node
    buildCommand: >-
      npm install &&
      BACKEND_URL=https://aio-tools-backend.onrender.com
      NEXT_PUBLIC_BACKEND_URL=https://aio-tools-backend.onrender.com
      NEXT_PUBLIC_SITE_URL=https://utilitytools.in
      npm run build --workspace frontend
    startCommand: bash scripts/start-frontend.sh
    envVars:
      - key: NODE_ENV
        value: production
      - key: NEXT_PUBLIC_SITE_URL
        value: https://utilitytools.in
      - key: BACKEND_URL
        value: https://aio-tools-backend.onrender.com
      - key: NEXT_PUBLIC_BACKEND_URL
        value: https://aio-tools-backend.onrender.com
